//Esto en un archivo de configuracion de reglas ESLint
//https://eslint.org/docs/latest/user-guide/configuring/configuration-files
{
    "rules": {
        // Override our default settings just for this directory
        "eqeqeq": "warn",
        "strict": "off",
        "quotes":["warn", "double"],
        "semi":["error", "never"],
        "no-dupe-keys" :"error"//no puede tener en un mismo objeto dos atributos con el mismo nombre
    }
}